# Backend

Run on Windows:

```bat
cd /d "C:\Users\Public\BusinessAutomation\AI ChatCAD\AI-Geometry-CAD-Assistant\backend"
run_backend.bat
```

API runs at `http://127.0.0.1:8020` and listens on `0.0.0.0` for local WiFi access.
